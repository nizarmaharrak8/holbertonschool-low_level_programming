# Dog Structure Project

This project defines a `struct dog` with the following members:
- `name` (char *): The name of the dog
- `age` (float): The age of the dog
- `owner` (char *): The owner of the dog

It also demonstrates how to initialize and print a `struct dog`.

